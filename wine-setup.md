Install
```console
$ lscpu
$ sudo apt install wine64 (sudo apt install wine32)
$ wine --version
```

Run
```console
$ winecfg
$ wine program.exe
```

Uninstall
```console
sudo apt purge wine32 (wine64)
sudo apt autoremove
```
